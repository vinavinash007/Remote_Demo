name: First DevOps Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print message
        run: echo "Hello DevOps Pipeline"

      - name: List files
        run: ls -la
      - name: Show current date
        run: date

      - name: Show branch name
        run: echo "Branch is $GITHUB_REF_NAME"